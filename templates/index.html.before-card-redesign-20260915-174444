<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<title>ASMAR MARKET | الأسمر ماركت</title>

<style>
:root{
    --black:#0b0a08;
    --black2:#211712;
    --gold:#a9823f;
    --gold2:#c99a45;
    --cream:#f1e6d2;
    --muted:#9b8d78;
    --border:rgba(201,154,69,.24);
    --sand:#c8b28a;
    --burgundy:#46151d;
    --emerald:#10382f;
}

*{box-sizing:border-box;margin:0;padding:0}

html{scroll-behavior:smooth}

body{
    font-family:Arial,"Tahoma",sans-serif;
    background:
        radial-gradient(circle at top,#fffdf5 0,#f5f6f8 38%,#eef0f3 100%);
    color:#171717;
    min-height:100vh;
}

a{text-decoration:none}

header{
    position:relative;
    overflow:hidden;
    background:
        radial-gradient(circle at 85% 0,rgba(212,175,55,.18),transparent 30%),
        linear-gradient(135deg,#080809,#19191d);
    color:#fff;
    padding:20px 15px 18px;
    border-bottom:1px solid rgba(212,175,55,.3);
    box-shadow:0 8px 30px rgba(0,0,0,.18);
}

header:after{
    content:"";
    position:absolute;
    width:170px;
    height:170px;
    left:-80px;
    top:-100px;
    border-radius:50%;
    border:1px solid rgba(212,175,55,.15);
}

.logo{
    position:relative;
    z-index:1;
    font-size:25px;
    font-weight:900;
    letter-spacing:1.5px;
}

.logo span{
    color:var(--gold);
    text-shadow:0 0 18px rgba(212,175,55,.25);
}

.search{
    position:relative;
    z-index:1;
    margin-top:17px;
    display:flex;
    background:#fff;
    border-radius:14px;
    padding:4px;
    box-shadow:0 8px 25px rgba(0,0,0,.22);
}

.search input{
    width:100%;
    min-width:0;
    padding:13px 14px;
    border:0;
    outline:0;
    border-radius:11px;
    font-size:14px;
    background:#fff;
}

.search button{
    flex:0 0 48px;
    border:0;
    border-radius:11px;
    background:linear-gradient(135deg,var(--gold2),var(--gold));
    color:#111;
    font-size:18px;
    font-weight:bold;
    cursor:pointer;
}

.categories{
    display:flex;
    gap:10px;
    overflow-x:auto;
    padding:14px 15px 16px;
    background:rgba(255,255,255,.92);
    border-bottom:1px solid #eee;
    scrollbar-width:none;
}

.categories::-webkit-scrollbar{display:none}

.category{
    position:relative;
    flex:0 0 92px;
    padding:7px 7px 10px;
    text-align:center;
    background:#fff;
    border:1px solid #eee;
    border-radius:16px;
    color:#111;
    box-shadow:0 5px 18px rgba(0,0,0,.06);
    overflow:hidden;
}

.category:active{
    transform:scale(.97);
}

.category img{
    width:100%;
    height:78px;
    object-fit:cover;
    display:block;
    border-radius:12px;
    margin-bottom:8px;
    background:#f2f2f2;
}

.category span{
    display:block;
    font-weight:800;
    font-size:12px;
    line-height:1.4;
    white-space:nowrap;
    overflow:hidden;
    text-overflow:ellipsis;
}

.hero{
    position:relative;
    overflow:hidden;
    margin:15px;
    padding:25px 20px;
    border-radius:24px;
    background:
        radial-gradient(circle at 15% 20%,rgba(212,175,55,.22),transparent 32%),
        linear-gradient(135deg,#09090b,#202025 60%,#111);
    color:#fff;
    border:1px solid rgba(212,175,55,.35);
    box-shadow:0 14px 35px rgba(0,0,0,.18);
}

.hero:before{
    content:"";
    position:absolute;
    width:190px;
    height:190px;
    left:-100px;
    bottom:-120px;
    border-radius:50%;
    border:1px solid rgba(212,175,55,.18);
}

.hero h1{
    position:relative;
    color:var(--gold2);
    font-size:24px;
    line-height:1.35;
    margin-bottom:10px;
}

.hero p{
    position:relative;
    color:#ddd;
    line-height:1.9;
    font-size:14px;
}

.section{
    padding:8px 15px 10px;
}

.section-title{
    margin-bottom:14px;
    display:flex;
    align-items:center;
    justify-content:space-between;
    gap:10px;
}

.section-title h2{
    font-size:19px;
    font-weight:900;
}

.section-title span{
    font-size:12px;
    color:#9b7800;
    background:#fff9e5;
    border:1px solid rgba(212,175,55,.25);
    padding:7px 10px;
    border-radius:20px;
    white-space:nowrap;
}

.products{
    display:grid;
    grid-template-columns:repeat(2,minmax(0,1fr));
    gap:11px;
}

.product{
    position:relative;
    min-width:0;
    background:rgba(255,255,255,.98);
    border-radius:20px;
    overflow:hidden;
    border:1px solid var(--border);
    box-shadow:0 8px 25px rgba(0,0,0,.08);
}

.product-image{
    height:190px;
    margin:7px;
    border-radius:15px;
    background:
        radial-gradient(circle at center,#fff,#f1f1f1);
    border:1px solid rgba(212,175,55,.18);
    display:flex;
    align-items:center;
    justify-content:center;
    overflow:hidden;
    position:relative;
}

.product-image:before{
    content:"";
    position:absolute;
    inset:0;
    background:linear-gradient(135deg,rgba(255,255,255,.8),transparent);
    pointer-events:none;
}

.product-image:after{
    content:"";
    position:absolute;
    inset:0;
    border-radius:15px;
    box-shadow:inset 0 0 25px rgba(0,0,0,.07);
    pointer-events:none;
}

.product-image img{
    width:100%;
    height:100%;
    object-fit:contain;
    display:block;
    padding:8px;
    position:relative;
    z-index:1;
}

.product-info{
    padding:11px 10px 12px;
    background:linear-gradient(to bottom,#fff,#fafafa);
}

.product-name{
    font-weight:900;
    margin-bottom:7px;
    font-size:14px;
    line-height:1.5;
    min-height:42px;
    display:-webkit-box;
    -webkit-line-clamp:2;
    -webkit-box-orient:vertical;
    overflow:hidden;
}

.price{
    color:#9d7800;
    font-weight:900;
    font-size:16px;
    line-height:1.5;
    margin-top:4px;
}

.seller{
    color:#777;
    font-size:11px;
    margin-top:7px;
    white-space:nowrap;
    overflow:hidden;
    text-overflow:ellipsis;
}

.cart-button{
    width:100%;
    margin-top:9px;
    padding:11px 7px;
    border:0;
    border-radius:11px;
    background:linear-gradient(135deg,#111,#29292d);
    color:#fff;
    font-size:12px;
    font-weight:900;
    box-shadow:0 5px 13px rgba(0,0,0,.14);
}

.cart-button:active{
    transform:scale(.97);
}

.join{
    margin:22px 15px 15px;
    padding:24px 18px;
    border-radius:22px;
    background:#fff;
    text-align:center;
    border:1px solid #eee;
    box-shadow:0 8px 25px rgba(0,0,0,.07);
}

.join h2{
    margin-bottom:9px;
    font-size:20px;
}

.join p{
    color:#666;
    line-height:1.8;
    font-size:14px;
}

.join button{
    margin-top:15px;
    padding:12px 26px;
    border:0;
    border-radius:12px;
    background:linear-gradient(135deg,var(--gold2),var(--gold));
    color:#111;
    font-weight:900;
    box-shadow:0 7px 18px rgba(212,175,55,.22);
}

footer{
    margin-top:28px;
    padding:28px 15px;
    background:
        radial-gradient(circle at 50% 0,rgba(212,175,55,.14),transparent 35%),
        #0b0b0d;
    color:#fff;
    text-align:center;
    border-top:1px solid rgba(212,175,55,.25);
}

footer strong{
    display:block;
    color:var(--gold);
    font-size:18px;
    letter-spacing:1px;
    margin-bottom:7px;
}

footer p{
    color:#aaa;
    font-size:12px;
}

@media (min-width:700px){
    header{
        padding:25px 7%;
    }

    .categories{
        padding-left:7%;
        padding-right:7%;
    }

    .hero{
        margin:22px 7%;
        padding:35px;
    }

    .section{
        padding-left:7%;
        padding-right:7%;
    }

    .products{
        grid-template-columns:repeat(3,minmax(0,1fr));
        gap:16px;
    }

    .product-image{
        height:250px;
    }

    .join{
        margin-left:7%;
        margin-right:7%;
    }
}

@media (min-width:1050px){
    .products{
        grid-template-columns:repeat(4,minmax(0,1fr));
    }

    .product-image{
        height:260px;
    }
}

/* ===== ASMAR LUXURY PRODUCT CARDS ===== */
.products-grid > div,
.product-card {
    position: relative;
    overflow: hidden;
    background: linear-gradient(145deg, #fffdf8 0%, #f1e6d2 100%);
    border: 1px solid rgba(169,130,63,.28);
    border-radius: 18px;
    box-shadow: 0 8px 24px rgba(33,23,18,.10);
    transition: transform .25s ease, box-shadow .25s ease, border-color .25s ease;
}

.products-grid > div:hover,
.product-card:hover {
    transform: translateY(-4px);
    border-color: rgba(201,154,69,.65);
    box-shadow: 0 14px 32px rgba(70,21,29,.16);
}

.products-grid > div::before,
.product-card::before {
    content: "";
    position: absolute;
    top: 0;
    right: 0;
    left: 0;
    height: 3px;
    background: linear-gradient(90deg, var(--burgundy), var(--gold2), var(--emerald));
    opacity: .9;
}

.products-grid img {
    transition: transform .35s ease;
}

.products-grid > div:hover img,
.product-card:hover img {
    transform: scale(1.035);
}

.products-grid .price,
.product-card .price {
    color: var(--burgundy) !important;
    font-weight: 900 !important;
}

.products-grid .seller,
.product-card .seller {
    color: var(--emerald) !important;
    font-weight: 800;
}

.products-grid .seller::first-letter,
.product-card .seller::first-letter {
    color: var(--gold2);
}

.products-grid button,
.product-card button {
    border-radius: 11px !important;
}

@media (max-width: 480px) {
    .products-grid > div,
    .product-card {
        border-radius: 16px;
        box-shadow: 0 6px 18px rgba(33,23,18,.09);
    }
}


/* ===== ASMAR PRODUCT CARDS - MOBILE POLISH ===== */
@media (max-width: 600px) {
    .products-grid > div,
    .product-card {
        padding-bottom: 12px !important;
        border-radius: 16px;
    }

    .products-grid > div img,
    .product-card img {
        max-height: 190px !important;
        object-fit: contain !important;
    }

    .products-grid > div h3,
    .products-grid > div h4,
    .product-card h3,
    .product-card h4 {
        margin-top: 10px !important;
        margin-bottom: 7px !important;
        line-height: 1.55 !important;
    }

    .products-grid .price,
    .product-card .price {
        margin: 7px 0 !important;
        line-height: 1.45 !important;
    }

    .products-grid .seller,
    .product-card .seller {
        margin: 7px 0 !important;
        font-size: 12px !important;
    }

    .products-grid > div button,
    .product-card button {
        min-height: 44px;
        font-weight: 800;
    }
}


/* ===== ASMAR LUXURY PLATFORM BACKGROUND ===== */
body {
    background:
        radial-gradient(circle at 8% 12%, rgba(70,21,29,.34) 0, transparent 28%),
        radial-gradient(circle at 92% 28%, rgba(16,56,47,.30) 0, transparent 30%),
        radial-gradient(circle at 45% 100%, rgba(201,154,69,.10) 0, transparent 32%),
        linear-gradient(135deg, #0b0a08 0%, #17110e 42%, #120d0c 68%, #0b0a08 100%) !important;
    background-attachment: fixed;
}


/* ===== ASMAR MARKET — REFERENCE LUXURY STYLE ===== */
html, body {
    min-height: 100%;
}

body {
    background:
        radial-gradient(ellipse at 0% 8%, rgba(255,255,255,.98) 0 18%, transparent 42%),
        radial-gradient(ellipse at 100% 18%, rgba(201,154,69,.18) 0 10%, transparent 34%),
        radial-gradient(ellipse at 15% 82%, rgba(255,255,255,.95) 0 15%, transparent 40%),
        radial-gradient(ellipse at 90% 90%, rgba(201,154,69,.14) 0 12%, transparent 38%),
        linear-gradient(135deg,#f8f7f4 0%,#eeeae3 42%,#faf9f7 70%,#e8e4dc 100%) !important;
    color:#211712 !important;
    position:relative;
    overflow-x:hidden;
}

body::before,
body::after {
    content:"";
    position:fixed;
    z-index:-1;
    pointer-events:none;
    border-radius:50%;
    filter:blur(1px);
}

body::before {
    width:75vw;
    height:32vh;
    top:8vh;
    left:-30vw;
    transform:rotate(-18deg);
    border-top:2px solid rgba(201,154,69,.55);
    border-bottom:7px solid rgba(255,255,255,.85);
    box-shadow:0 0 35px rgba(201,154,69,.12);
}

body::after {
    width:80vw;
    height:38vh;
    bottom:4vh;
    right:-35vw;
    transform:rotate(-20deg);
    border-top:2px solid rgba(201,154,69,.48);
    border-bottom:8px solid rgba(255,255,255,.9);
    box-shadow:0 0 40px rgba(169,130,63,.12);
}

/* Luxury header */
header, .header, .navbar, nav {
    position:relative;
}

header::after,
.navbar::after {
    content:"";
    display:block;
    height:1px;
    margin:0 auto;
    max-width:92%;
    background:linear-gradient(90deg,transparent,#c99a45,#f1e6d2,#c99a45,transparent);
    opacity:.7;
}

/* Main content */
main, .container, .content {
    position:relative;
    z-index:1;
}

/* Product cards */
.products-grid > div,
.product-card {
    background:linear-gradient(145deg,rgba(255,255,255,.97),rgba(241,230,210,.88)) !important;
    border:1px solid rgba(169,130,63,.32) !important;
    box-shadow:0 12px 30px rgba(33,23,18,.12) !important;
    backdrop-filter:blur(8px);
}

.products-grid > div:hover,
.product-card:hover {
    box-shadow:0 18px 38px rgba(33,23,18,.18) !important;
}

/* Luxury section headings */
h1,h2,h3,h4 {
    color:#211712;
}

.price {
    color:#46151d !important;
}

/* Search */
input[type="search"],
input[placeholder*="بحث"],
.search-box input {
    background:rgba(255,255,255,.92) !important;
    border:1px solid rgba(169,130,63,.4) !important;
    box-shadow:0 5px 20px rgba(33,23,18,.08);
}

/* Buttons */
button,
.btn {
    border-radius:999px !important;
}

/* Soft premium sections */
section {
    position:relative;
}


/* ===== ASMAR FINAL PRODUCT CARD SPACING ===== */
.products-grid > div,
.product-card {
    padding: 12px !important;
    overflow: hidden;
}

.products-grid > div h3,
.products-grid > div h4,
.product-card h3,
.product-card h4 {
    margin: 8px 0 6px !important;
    line-height: 1.35 !important;
}

.products-grid .price,
.product-card .price {
    margin: 5px 0 !important;
    line-height: 1.3 !important;
}

.products-grid .seller,
.product-card .seller {
    margin: 4px 0 8px !important;
    line-height: 1.35 !important;
}

.products-grid > div button,
.product-card button {
    margin-top: 6px !important;
}


/* ===== ASMAR FINAL PRODUCT CARD INNER SPACING ===== */
.products .product {
    padding: 0 !important;
    overflow: hidden;
}

.products .product-info {
    padding: 10px 11px 12px !important;
}

.products .product-name {
    margin: 0 0 6px !important;
    line-height: 1.4 !important;
}

.products .product-info > .price {
    margin: 0 0 7px !important;
    line-height: 1.35 !important;
}

.products .product-info > .seller {
    margin: 7px 0 8px !important;
    line-height: 1.4 !important;
}

.products .product-info > form {
    margin: 0 !important;
}

.products .product-info .cart-button {
    margin: 0 !important;
}

</style>
<link rel="stylesheet" href="{{ url_for('static', filename='css/asmar-target.css') }}">
<style>
.product-categories{
    display:flex;
    gap:10px;
    padding:14px 4px;
    margin:10px 0 18px;
    overflow-x:auto;
    scrollbar-width:none;
    direction:rtl;
}
.product-categories::-webkit-scrollbar{display:none}
.product-category{
    flex:0 0 auto;
    display:flex;
    align-items:center;
    gap:7px;
    padding:10px 15px;
    border-radius:14px;
    text-decoration:none;
    background:rgba(255,255,255,.72);
    color:var(--black2,#222);
    border:1px solid rgba(212,175,55,.28);
    box-shadow:0 4px 14px rgba(0,0,0,.06);
    font-weight:700;
    transition:.2s ease;
}
.product-category.active{
    background:linear-gradient(135deg,var(--gold2,#f3dfad),var(--gold,#d4af37));
    color:var(--black,#111);
    border-color:#d4af37;
}
.product-category:hover{
    transform:translateY(-2px);
}
</style>

</head>

<body>

<header>

<div class="logo">
ASMAR <span>MARKET</span>
</div>

<div style="display:flex;gap:8px;align-items:center;justify-content:center;margin:10px 0;flex-wrap:wrap;">
    <a href="/cart" style="text-decoration:none;background:var(--black2);color:var(--cream);padding:9px 14px;border-radius:10px;">
        السلة
    </a>

    {% if session.get("customer_id") %}
        <a href="/customer/account" style="text-decoration:none;background:var(--black2);color:var(--gold2);padding:9px 14px;border-radius:10px;">👤 حسابي</a>
        <a href="/customer/notifications" style="text-decoration:none;background:var(--black2);color:var(--gold2);padding:9px 14px;border-radius:10px;">
            🔔 إشعاراتي
            {% if unread_customer_notifications > 0 %}
                <span style="background:red;color:white;border-radius:50%;padding:4px 9px;margin-right:5px;font-size:14px;">
                    {{ unread_customer_notifications }}
                </span>
            {% endif %}
        </a>
        <a href="/customer/orders" style="text-decoration:none;background:var(--black2);color:var(--cream);padding:9px 14px;border-radius:10px;">
            مشترياتي
        </a>
        <span style="font-size:13px;color:#555;">
            👤 {{ session.get("customer_name") }}
        </span>
        <a href="/customer/logout" style="text-decoration:none;background:var(--burgundy);color:var(--cream);padding:9px 14px;border-radius:10px;">
            🚪 خروج
        </a>
    {% else %}
        <a href="/customer/login" style="text-decoration:none;background:var(--black2);color:var(--cream);padding:9px 14px;border-radius:10px;">
            🔐 دخول
        </a>
        <a href="/customer/register" style="text-decoration:none;background:var(--sand);color:var(--black);padding:9px 14px;border-radius:10px;">
            إنشاء حساب
        </a>
    {% endif %}
</div>

{% if session.get("customer_id") %}
<div style="margin:12px 0 4px;text-align:center;">
    <form method="POST" action="/follow" style="display:inline;">
        <input type="hidden" name="csrf_token" value="{{ csrf_token }}">
        {% if is_following %}
        <button type="submit" style="border:1px solid #d4af37;background:linear-gradient(135deg,var(--gold2),var(--gold));color:var(--black);padding:10px 18px;border-radius:12px;font-weight:bold;">
            ✓ أنت تتابع ASMAR MARKET
        </button>
        {% else %}
        <button type="submit" style="border:1px solid #d4af37;background:var(--black2);color:var(--gold2);padding:10px 18px;border-radius:12px;font-weight:bold;">
            ➕ متابعة ASMAR MARKET
        </button>
        {% endif %}
    </form>
</div>
{% endif %}

<form class="search" method="GET" action="/">
<input
    type="text"
    name="search"
    value="{{ search or '' }}"
    placeholder="ابحث عن منتج أو متجر..."
    autocomplete="off"
>
<button type="submit">🔍</button>
</form>

</header>

<section class="hero">

<h1>مرحبًا بك في ASMAR MARKET</h1>

<p>
سوق إلكتروني يجمع التجار والمنتجات من اليمن
والخليج والعالم في مكان واحد.
</p>

</section>

<!-- ASMAR MARKET — PRODUCT CATEGORIES -->
<section class="product-categories" aria-label="تصنيفات المنتجات">
    <a href="/" class="product-category {% if not category %}active{% endif %}">
        <span>الكل</span>
    </a>
    <a href="/?category=أزياء" class="product-category {% if category == 'أزياء' %}active{% endif %}">
        <span>أزياء وملابس</span>
    </a>
    <a href="/?category=أحذية وحقائب" class="product-category {% if category == 'أحذية وحقائب' %}active{% endif %}">
        <span>أحذية وحقائب</span>
    </a>
    <a href="/?category=مجوهرات وإكسسوارات" class="product-category {% if category == 'مجوهرات وإكسسوارات' %}active{% endif %}">
        <span>مجوهرات وإكسسوارات</span>
    </a>
    <a href="/?category=جمال وعناية" class="product-category {% if category == 'جمال وعناية' %}active{% endif %}">
        <span>جمال وعناية</span>
    </a>
    <a href="/?category=إلكترونيات" class="product-category {% if category == 'إلكترونيات' %}active{% endif %}">
        <span>إلكترونيات وتقنية</span>
    </a>
    <a href="/?category=منزل وأثاث" class="product-category {% if category == 'منزل وأثاث' %}active{% endif %}">
        <span>منزل وأثاث</span>
    </a>
    <a href="/?category=مطبخ" class="product-category {% if category == 'مطبخ' %}active{% endif %}">
        <span>مطبخ</span>
    </a>
    <a href="/?category=مفروشات وديكور" class="product-category {% if category == 'مفروشات وديكور' %}active{% endif %}">
        <span>مفروشات وديكور</span>
    </a>
    <a href="/?category=أطفال وأمهات" class="product-category {% if category == 'أطفال وأمهات' %}active{% endif %}">
        <span>أطفال وأمهات</span>
    </a>
    <a href="/?category=ألعاب وهوايات" class="product-category {% if category == 'ألعاب وهوايات' %}active{% endif %}">
        <span>ألعاب وهوايات</span>
    </a>
    <a href="/?category=رياضة ولياقة" class="product-category {% if category == 'رياضة ولياقة' %}active{% endif %}">
        <span>رياضة ولياقة</span>
    </a>
    <a href="/?category=سيارات ومركبات" class="product-category {% if category == 'سيارات ومركبات' %}active{% endif %}">
        <span>سيارات ومركبات</span>
    </a>
    <a href="/?category=أدوات ومعدات" class="product-category {% if category == 'أدوات ومعدات' %}active{% endif %}">
        <span>أدوات ومعدات</span>
    </a>
    <a href="/?category=كتب وتعليم" class="product-category {% if category == 'كتب وتعليم' %}active{% endif %}">
        <span>كتب وتعليم</span>
    </a>
    <a href="/?category=أغذية ومشروبات" class="product-category {% if category == 'أغذية ومشروبات' %}active{% endif %}">
        <span>أغذية ومشروبات</span>
    </a>
    <a href="/?category=عطور وهدايا" class="product-category {% if category == 'عطور وهدايا' %}active{% endif %}">
        <span>عطور وهدايا</span>
    </a>
    <a href="/?category=حيوانات" class="product-category {% if category == 'حيوانات' %}active{% endif %}">
        <span>حيوانات</span>
    </a>
    <a href="/?category=زراعة وحدائق" class="product-category {% if category == 'زراعة وحدائق' %}active{% endif %}">
        <span>زراعة وحدائق</span>
    </a>
    <a href="/?category=مكاتب وتجاري" class="product-category {% if category == 'مكاتب وتجاري' %}active{% endif %}">
        <span>مكاتب وتجاري</span>
    </a>
    <a href="/?category=خدمات" class="product-category {% if category == 'خدمات' %}active{% endif %}">
        <span>خدمات</span>
    </a>
    <a href="/?category=مستعمل" class="product-category {% if category == 'مستعمل' %}active{% endif %}">
        <span>مستعمل</span>
    </a>
    <a href="/?category=أخرى" class="product-category {% if category == 'أخرى' %}active{% endif %}">
        <span>أخرى</span>
    </a>
</section>
<!-- END ASMAR CATEGORIES -->

<div class="ad-list">

{% for ad in ads %}
<a class="ad-card"
   href="{{ ad['link'] }}"
   style="--ad-card-width: {{ ad['card_width'] or 80 }}px;
          --ad-card-height: {{ ad['card_height'] or 50 }}px;
          --ad-image-width: {{ ad['image_width'] or 74 }}px;
          --ad-image-height: {{ ad['image_height'] or 30 }}px;">

{% if ad['image'] %}
<img loading="lazy" src="{{ url_for('ad_file', filename=ad['image']) }}"
     alt="{{ ad['title'] }}">
{% endif %}

<span>{{ ad['title'] }}</span>

</a>
{% endfor %}

</div>








<section class="section">

<div class="section-title">
<h2>🔥 منتجات مميزة</h2>
<span>عرض الكل</span>
</div>












<div class="products">

{% for product in products %}

<div class="product">

<a href="{{ url_for('product_details', product_id=product['id']) }}"
style="display:block;text-decoration:none;color:inherit;">
<div class="product-image"
style="--merchant-x: {{ product['image_x'] or 0 }}px; --merchant-y: {{ product['image_y'] or 0 }}px; --merchant-zoom: {{ product['image_zoom'] or 1 }};">
{% if product["image"] %}
<img loading="lazy" src="{{ url_for('static', filename='uploads/' + product['image']) }}"
style="width:100%;height:100%;object-fit:contain;display:block;transform:translate(var(--merchant-x), var(--merchant-y)) scale(var(--merchant-zoom));transform-origin:center center;">
{% else %}
صورة المنتج
{% endif %}
</div>
</a>

<div class="product-info">

<div class="product-name">
{{ product["name"] }}
</div>

<div class="price">
{% if product["original_price"] and product["original_price"]|float > product["price"]|float %}
    <span class="old-price" style="text-decoration:line-through;color:#999;font-size:13px;margin-left:7px;">
    {% if product["currency"] == "YER" %}
        {{ product["original_price"] }} ريال يمني
    {% elif product["currency"] == "SAR" %}
        {{ product["original_price"] }} ريال سعودي
    {% elif product["currency"] == "USD" %}
        ${{ product["original_price"] }} دولار أمريكي
    {% elif product["currency"] == "AED" %}
        {{ product["original_price"] }} درهم إماراتي
    {% elif product["currency"] == "EGP" %}
        {{ product["original_price"] }} جنيه مصري
    {% elif product["currency"] == "KWD" %}
        {{ product["original_price"] }} دينار كويتي
    {% elif product["currency"] == "EUR" %}
        €{{ product["original_price"] }} يورو
    {% elif product["currency"] == "GBP" %}
        £{{ product["original_price"] }} جنيه إسترليني
    {% else %}
        {{ product["original_price"] }} {{ product["currency"] }}
    {% endif %}
    </span>
{% endif %}

<span class="current-price">
{% if product["currency"] == "YER" %}
    🇾🇪 {{ product["price"] }} ريال يمني
{% elif product["currency"] == "SAR" %}
    🇸🇦 {{ product["price"] }} ريال سعودي
{% elif product["currency"] == "USD" %}
    🇺🇸 ${{ product["price"] }} دولار أمريكي
{% elif product["currency"] == "AED" %}
    🇦🇪 {{ product["price"] }} درهم إماراتي
{% elif product["currency"] == "EGP" %}
    🇪🇬 {{ product["price"] }} جنيه مصري
{% elif product["currency"] == "KWD" %}
    🇰🇼 {{ product["price"] }} دينار كويتي
{% elif product["currency"] == "EUR" %}
    🇪🇺 €{{ product["price"] }} يورو
{% elif product["currency"] == "GBP" %}
    🇬🇧 £{{ product["price"] }} جنيه إسترليني
{% else %}
    🌍 {{ product["price"] }} {{ product["currency"] }}
{% endif %}
</span>
</div>

  {% set likes = product["likes_count"]|int %}
  {% if likes >= 60 %}{% set rating_stars = 5 %}
  {% elif likes >= 30 %}{% set rating_stars = 4 %}
  {% elif likes >= 15 %}{% set rating_stars = 3 %}
  {% elif likes >= 5 %}{% set rating_stars = 2 %}
  {% elif likes >= 1 %}{% set rating_stars = 1 %}
  {% else %}{% set rating_stars = 0 %}{% endif %}
  <div style="margin:8px 0 9px;text-align:center;">
      <div style="font-size:17px;letter-spacing:2px;line-height:1;">{% for i in range(1,6) %}{% if i <= rating_stars %}<span style="color:#d4af37;">★</span>{% else %}<span style="color:#d8d8d8;">☆</span>{% endif %}{% endfor %}</div>
      <div style="height:2px;margin:6px auto 0;width:82%;background:linear-gradient(to right,#d4af37 {{ (rating_stars / 5 * 100)|round(0) }}%,#e5e5e5 {{ (rating_stars / 5 * 100)|round(0) }}%);border-radius:10px;"></div>
  </div>
<div style="display:flex;justify-content:space-between;align-items:center;gap:5px;margin:9px 0;padding:7px 8px;background:#f7f7f7;border:1px solid #eee;border-radius:10px;font-size:11px;color:#666;">
    <span>👁️ {{ product["views_count"] }} مشاهدة</span>

    {% if session.get("customer_id") %}
    <form action="/product/{{ product['id'] }}/like" method="POST" style="margin:0;">
        <input type="hidden" name="csrf_token" value="{{ csrf_token }}">
        <button type="submit"
                style="border:0;background:none;cursor:pointer;font-size:13px;color:#666;padding:0;">
            ❤️ {{ product["likes_count"] }} إعجاب
        </button>
    </form>
    {% else %}
    <a href="/customer/login"
       style="text-decoration:none;color:#666;font-size:13px;">
        ❤️ {{ product["likes_count"] }} إعجاب
    </a>
    {% endif %}
</div>

<div class="seller">
    🏪 {{ product["merchant_name"] or "تاجر على ASMAR MARKET" }}
</div>

<form action="/cart/add/{{ product['id'] }}" method="POST">
    <input type="hidden" name="csrf_token" value="{{ csrf_token }}">
    <button type="submit" class="cart-button">
        🛒 أضف إلى السلة
    </button>
</form>

</div>

</div>

{% else %}

<p>لا توجد منتجات حاليًا.</p>

{% endfor %}

</div>

</section>

<section class="asmar-features" aria-label="مميزات أسمر ماركت">

  <div class="asmar-feature">
    <div class="feature-icon">🚚</div>
    <div class="feature-text">
      <strong>شحن سريع</strong>
      <span>توصيل إلى باب بيتك</span>
    </div>
  </div>

  <div class="asmar-feature">
    <div class="feature-icon">🔒</div>
    <div class="feature-text">
      <strong>دفع آمن</strong>
      <span>طرق دفع متعددة وآمنة</span>
    </div>
  </div>

  <div class="asmar-feature">
    <div class="feature-icon">✓</div>
    <div class="feature-text">
      <strong>ضمان الجودة</strong>
      <span>منتجات موثوقة وأصلية</span>
    </div>
  </div>

  <div class="asmar-feature">
    <div class="feature-icon">🏷</div>
    <div class="feature-text">
      <strong>عروض حصرية</strong>
      <span>أفضل الأسعار والخصومات</span>
    </div>
  </div>

</section>

<section style="margin:25px 15px 22px;background:linear-gradient(135deg,#09090b,#202025);color:#fff;border-radius:22px;padding:25px 18px;text-align:center;border:1px solid rgba(212,175,55,.5);box-shadow:0 12px 30px rgba(0,0,0,.16);">
    <div style="width:58px;height:58px;margin:0 auto 12px;border-radius:18px;background:rgba(212,175,55,.12);border:1px solid rgba(212,175,55,.35);display:flex;align-items:center;justify-content:center;font-size:28px;">📩</div>
    <h2 style="margin:0 0 9px;color:#f0cf67;font-size:20px;">الشكاوى والدعم</h2>
    <p style="margin:0 auto 17px;color:#ddd;line-height:1.8;font-size:13px;max-width:420px;">
        تحتاج مساعدة؟ فريق إدارة ASMAR MARKET موجود لمتابعة شكواك ومساعدتك.
    </p>
    <a href="/complaints"
       style="display:inline-block;text-decoration:none;background:linear-gradient(135deg,#f0cf67,#d4af37);color:#111;font-weight:900;padding:12px 22px;border-radius:12px;box-shadow:0 7px 18px rgba(212,175,55,.22);">
        فتح مركز الشكاوى والدعم
    </a>
</section>

<section class="join">

<h2>🛍 هل أنت تاجر؟</h2>

<p>
اعرض منتجاتك أمام عملاء من اليمن والخليج والعالم.
</p>

<a href="/merchant/register" style="display:inline-block;position:relative;z-index:10000;pointer-events:auto;cursor:pointer;text-decoration:none;background:linear-gradient(135deg,#f0cf67,#d4af37);color:#111;font-weight:900;padding:12px 24px;border-radius:12px;box-shadow:0 7px 18px rgba(212,175,55,.22);">انضم كتاجر</a>

</section>

<footer>

<strong>ASMAR MARKET</strong>

<p>سوق واحد... فرص بلا حدود 🌍</p>

</footer>

</body>
</html>

<style>
/* =========================================================
   ASMAR MARKET — LUXURY PRODUCT CARDS v2
   Responsive: Mobile + Desktop
   Visual only — no functionality changes
   ========================================================= */

.products{
    align-items:stretch;
}

.products .product{
    position:relative;
    overflow:hidden;
    display:flex;
    flex-direction:column;
    min-width:0;
    background:
        linear-gradient(145deg,rgba(255,255,255,.98),rgba(246,239,226,.98));
    border:1px solid rgba(191,151,70,.30);
    border-radius:22px;
    box-shadow:
        0 8px 28px rgba(0,0,0,.12),
        inset 0 1px 0 rgba(255,255,255,.85);
    transition:
        transform .28s ease,
        box-shadow .28s ease,
        border-color .28s ease;
}

.products .product::before{
    content:"";
    position:absolute;
    top:0;
    left:12%;
    right:12%;
    height:2px;
    z-index:3;
    background:linear-gradient(
        90deg,
        transparent,
        #d4af37,
        #f3d98a,
        #d4af37,
        transparent
    );
    opacity:.9;
}

.products .product::after{
    content:"";
    position:absolute;
    inset:0;
    pointer-events:none;
    border-radius:22px;
    box-shadow:inset 0 0 0 1px rgba(255,255,255,.35);
}

.products .product:hover{
    transform:translateY(-6px);
    border-color:rgba(212,175,55,.65);
    box-shadow:
        0 18px 42px rgba(0,0,0,.18),
        0 0 0 1px rgba(212,175,55,.08);
}

.products .product > a{
    position:relative;
    overflow:hidden;
    border-radius:21px 21px 0 0;
}

.products .product-image{
    position:relative;
    height:270px;
    min-height:270px;
    display:flex;
    align-items:center;
    justify-content:center;
    overflow:hidden;
    background:
        radial-gradient(circle at 50% 42%,#ffffff 0%,#faf6ed 52%,#eee3d1 100%);
}

.products .product-image::after{
    content:"";
    position:absolute;
    left:12%;
    right:12%;
    bottom:10px;
    height:24px;
    background:radial-gradient(
        ellipse,
        rgba(0,0,0,.14),
        transparent 70%
    );
    filter:blur(8px);
    opacity:.55;
    pointer-events:none;
}

.products .product-image img{
    width:100% !important;
    height:100% !important;
    object-fit:contain !important;
    padding:14px;
    transition:transform .45s cubic-bezier(.2,.7,.2,1);
    filter:drop-shadow(0 9px 12px rgba(0,0,0,.12));
}

.products .product:hover .product-image img{
    transform:scale(1.055);
}

.products .product-info{
    position:relative;
    z-index:2;
    display:flex;
    flex-direction:column;
    flex:1;
    padding:14px 15px 16px;
    background:linear-gradient(
        180deg,
        rgba(255,255,255,.82),
        rgba(250,245,236,.96)
    );
}

.products .product-name{
    min-height:44px;
    display:flex;
    align-items:center;
    justify-content:center;
    margin:0 0 8px;
    color:#241d19;
    font-size:16px;
    font-weight:900;
    line-height:1.55;
    text-align:center;
}

.products .product-info > .price{
    margin:2px 0 8px;
    padding:8px 7px;
    color:#741f2a !important;
    font-size:17px;
    font-weight:950 !important;
    line-height:1.45;
    text-align:center;
    background:linear-gradient(
        135deg,
        rgba(212,175,55,.10),
        rgba(255,255,255,.7)
    );
    border:1px solid rgba(212,175,55,.20);
    border-radius:12px;
}

.products .product-info > div[style*="margin:8px 0 9px"]{
    margin:7px 0 9px !important;
}

.products .product-info > div[style*="background:#f7f7f7"]{
    margin:8px 0 10px !important;
    padding:8px 9px !important;
    background:rgba(255,255,255,.72) !important;
    border:1px solid rgba(212,175,55,.18) !important;
    border-radius:12px !important;
    box-shadow:0 3px 10px rgba(0,0,0,.04);
}

.products .seller{
    margin:8px 0 11px;
    padding:7px 9px;
    overflow:hidden;
    color:#126052 !important;
    font-size:12px;
    font-weight:850;
    line-height:1.5;
    text-align:center;
    text-overflow:ellipsis;
    white-space:nowrap;
    background:rgba(16,80,67,.055);
    border:1px solid rgba(16,80,67,.12);
    border-radius:10px;
}

.products .product-info > form:last-child{
    margin-top:auto;
}

.products .product-info .cart-button{
    width:100%;
    min-height:46px;
    padding:10px 14px;
    border:1px solid rgba(212,175,55,.65) !important;
    border-radius:13px !important;
    color:#fff !important;
    font-size:14px;
    font-weight:900;
    background:linear-gradient(
        135deg,
        #191513,
        #3a2720 48%,
        #171210
    ) !important;
    box-shadow:
        0 6px 16px rgba(0,0,0,.16),
        inset 0 1px 0 rgba(255,255,255,.10);
    transition:
        transform .2s ease,
        box-shadow .2s ease,
        border-color .2s ease;
}

.products .product-info .cart-button:hover{
    transform:translateY(-2px);
    border-color:#e4c566 !important;
    box-shadow:
        0 9px 20px rgba(0,0,0,.22),
        0 0 18px rgba(212,175,55,.10);
}

.products .product-info .cart-button:active{
    transform:translateY(0);
}

/* ---------- Desktop ---------- */
@media (min-width:769px){
    .products{
        gap:20px;
    }

    .products .product{
        min-height:530px;
    }
}

/* ---------- Tablet ---------- */
@media (min-width:481px) and (max-width:768px){
    .products{
        gap:15px;
    }

    .products .product-image{
        height:230px;
        min-height:230px;
    }

    .products .product-info{
        padding:12px;
    }

    .products .product-name{
        font-size:15px;
    }

    .products .product-info > .price{
        font-size:16px;
    }
}

/* ---------- Mobile ---------- */
@media (max-width:480px){
    .products{
        gap:10px !important;
    }

    .products .product{
        border-radius:17px;
        box-shadow:
            0 5px 18px rgba(0,0,0,.10),
            inset 0 1px 0 rgba(255,255,255,.8);
    }

    .products .product::after{
        border-radius:17px;
    }

    .products .product:hover{
        transform:none;
    }

    .products .product > a{
        border-radius:16px 16px 0 0;
    }

    .products .product-image{
        height:185px !important;
        min-height:185px !important;
    }

    .products .product-image img{
        padding:9px;
    }

    .products .product-info{
        padding:10px 9px 11px;
    }

    .products .product-name{
        min-height:40px;
        margin-bottom:5px;
        font-size:13px;
        line-height:1.5;
    }

    .products .product-info > .price{
        margin:2px 0 6px;
        padding:6px 4px;
        font-size:13px;
        border-radius:10px;
    }

    .products .product-info > div[style*="margin:8px 0 9px"]{
        margin:5px 0 7px !important;
    }

    .products .product-info > div[style*="background:#f7f7f7"]{
        margin:6px 0 7px !important;
        padding:6px 5px !important;
        font-size:10px !important;
        border-radius:9px !important;
    }

    .products .seller{
        margin:6px 0 8px;
        padding:5px 6px;
        font-size:10px;
        border-radius:8px;
    }

    .products .product-info .cart-button{
        min-height:42px;
        padding:8px 7px;
        font-size:12px;
        border-radius:11px !important;
    }
}

/* ---------- Very small phones ---------- */
@media (max-width:360px){
    .products{
        gap:8px !important;
    }

    .products .product-image{
        height:165px !important;
        min-height:165px !important;
    }

    .products .product-info{
        padding:8px 7px 9px;
    }

    .products .product-name{
        font-size:12px;
    }

    .products .product-info > .price{
        font-size:12px;
    }

    .products .seller{
        font-size:9px;
    }

    .products .product-info .cart-button{
        font-size:11px;
        min-height:40px;
    }
}
</style>

<style>
/* =========================================
   ASMAR MARKET — Luxury Category Typography
   ========================================= */

.product-categories .product-category {
    position: relative;
    font-family: "Cairo", "Tajawal", "Noto Kufi Arabic", sans-serif;
    font-weight: 700;
    letter-spacing: .1px;
    color: #d6b25e;
    text-shadow:
        0 0 8px rgba(214,178,94,.18),
        0 0 18px rgba(214,178,94,.08);
    transition:
        color .35s ease,
        text-shadow .35s ease,
        transform .35s ease;
}

.product-categories .product-category span {
    display: inline-block;
    background: linear-gradient(
        90deg,
        #a77b22 0%,
        #f7df91 28%,
        #fff3bd 50%,
        #d6b25e 72%,
        #9a6f1d 100%
    );
    background-size: 220% auto;
    background-position: 100% center;
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    transition:
        background-position .7s ease,
        filter .35s ease;
}

.product-categories .product-category:hover,
.product-categories .product-category.active {
    transform: translateY(-1px);
    text-shadow:
        0 0 10px rgba(247,223,145,.45),
        0 0 22px rgba(214,178,94,.22);
}

.product-categories .product-category:hover span,
.product-categories .product-category.active span {
    background-position: 0 center;
    filter: brightness(1.15);
}

/* خط ذهبي فاخر أسفل التصنيف النشط */
.product-categories .product-category.active::after {
    content: "";
    position: absolute;
    left: 18%;
    right: 18%;
    bottom: -6px;
    height: 2px;
    border-radius: 999px;
    background: linear-gradient(
        90deg,
        transparent,
        #d6b25e,
        #fff1a8,
        #d6b25e,
        transparent
    );
    box-shadow:
        0 0 7px rgba(214,178,94,.55),
        0 0 14px rgba(214,178,94,.22);
}

@media (max-width: 600px) {
    .product-categories .product-category {
        font-size: 13px;
    }
}
</style>
